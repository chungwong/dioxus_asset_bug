use dioxus_lib::prelude::*;

use crate::Styled;

#[component]
#[allow(clippy::missing_errors_doc)]
pub fn DatePicker() -> Element {
    rsx! {
        Styled {
            div {
            }
        }
    }
}
